import React from "react";

import { HeaderContainer, Logo } from "./styled";

export const Header = () => {
  return (
    <HeaderContainer>
      <Logo>coffee time</Logo>
    </HeaderContainer>
  );
};
