import React from "react";
import { StyleSheet, View, Text } from "react-native";

export const Data = ({ recipe }) => {
    // recipe === "chemex" ?
    return (
        <View>
            <Text></Text>
        </View>
    );
};
